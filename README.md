# AI Document Processing Pipeline

## Overview
This project processes legal contracts (PDFs) to extract specific clauses (Termination, Confidentiality, Liability) and generates a summary using LLMs.

## Setup Instructions

### 1. Installation
1. Install dependencies:
   ```bash
   pip install -r requirements.txt